# Render deployment configuration for Techohaven Website
services:
  - type: static
    name: techohaven-website
    buildCommand: npm install && npx vite build
    staticPublishPath: dist
    envVars:
      - key: NODE_VERSION
        value: 18
